import { SearchFilterPlayerPipe } from './search-filter-player.pipe';

describe('SearchFilterPlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterPlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
